import * as React from "react";
import { Dispatcher, DispatcherRegisterOptions } from "@storex/core";
export interface SubscribeProps {
    to: Dispatcher | DispatcherRegisterOptions | (Dispatcher | DispatcherRegisterOptions)[];
    children: () => any;
}
export declare class Subscribe extends React.Component<SubscribeProps> {
    isUpdated: boolean;
    mounted: boolean;
    state: {
        dispatchers: any;
    };
    update: (state: any) => void;
    componentWillMount(): void;
    componentWillUnmount(): void;
    render(): any;
}
