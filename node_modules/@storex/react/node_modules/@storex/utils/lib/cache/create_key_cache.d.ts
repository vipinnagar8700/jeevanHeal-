import { KeyCache } from "./key_cache";
import { KeyCacheLimiter } from "./key_cache_limiter";
import { FetchTracker } from "../fetcher";
declare type createKeyValueCacheArgs<T> = {
    limit?: number;
    wait_map?: (val: T) => any;
    default_value?: () => T | null;
};
export declare function createKeyCache<T>({ limit, wait_map, default_value }?: createKeyValueCacheArgs<T>): {
    limiter: KeyCacheLimiter;
    cache: KeyCache<T>;
    tracker: FetchTracker<T>;
};
export {};
